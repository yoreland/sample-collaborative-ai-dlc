---
name: bmad-testarch-test-design
description: >
  Design the system-level test strategy (coverage inventory, gap analysis,
  risk-based priorities) plus the acceptance-framework plan, from PRD +
  architecture. Frameworks are planned here, not installed.
argument-hint: '[mode: create | resume | validate | edit]'
user-invocable: true
classification: write
---

# /bmad-testarch-test-design

Murat's risk assessment + epic-level test plan from PRD + architecture (both
final). Produces the test strategy and the acceptance-framework plan, including
when the framework must be ready (readiness by risk, not story number). May run
in parallel with story creation.
