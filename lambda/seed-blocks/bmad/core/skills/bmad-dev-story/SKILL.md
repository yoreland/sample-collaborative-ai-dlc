---
name: bmad-dev-story
description: >
  Implement a single story, turning the red acceptance tests green via an internal
  red-green-refactor loop and writing unit tests alongside the implementation.
argument-hint: '[story id]'
user-invocable: true
classification: write
---

# /bmad-dev-story

Turn red acceptance tests green. Unit tests are written here alongside the
implementation. Respect protected paths and frozen contracts (byte-frozen,
additive only).
