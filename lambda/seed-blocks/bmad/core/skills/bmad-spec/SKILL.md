---
name: bmad-spec
description: >
  Distill the ARCHITECTURE-SPINE + PRD into a SPEC.md contract that locks WHAT for
  downstream reference. Writes version requirements, not machine paths.
argument-hint: ''
user-invocable: true
classification: write
---

# /bmad-spec

Optional (v6.8+). Distill architecture + PRD into a SPEC contract that downstream
stages reference. The architecture run recommends this as its next step.
